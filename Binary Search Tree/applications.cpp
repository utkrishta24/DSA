/*
> for balanced BSTs:
> to maintain sorted stream of data( or sorted set of data)
> to implement doubly ended priority queue
> to solve problems like:
    > count smaller/greater in a stream
    > floor/ceil/smaller/greater in a stream
> if we have only search,insert,delete or their subsets-> use hash table rather than BST
*/